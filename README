 Projet
  1. Architecture
     *Packages
- model : contient les classes métier (`Project`, `Entity`, `Attribute`).
- generator : contient la logique de génération de données (`DatasetGenerator`, `DataGenerator`, `ValueProvider`).
- export : contient les classes pour exporter les datasets dans différents formats (`CsvExporter`, `JsonExporter`, `XmlExporter`, `SqlExporter`).

 2. Principes SOLID respectés
   *SRP — Single Responsibility Principle
   Chaque classe a une responsabilité unique :
   Attribute / Entity / Project : modèle des métadonnées.
   RandomDataGenerator : génération aléatoire.
   CSVExporter / JSONExporter / XMLExporter / SQLExporter : exportation (une responsabilité par classe).
   DatasetGenerator : orchestration de la génération pour un Project.

   *OCP — Open/Closed Principle
   Ajout de nouvelles fonctionnalités (nouveau Exporter, nouveau DataGenerator, nouveau ValueProvider) sans modifier le cœur existant.

   *LSP — Liskov Substitution Principle
   Toutes les implémentations d'une interface (Exporter, DataGenerator, ValueProvider) peuvent remplacer l'interface sans casser le comportement attendu.

   *ISP — Interface Segregation Principle
   Les interfaces sont fines et orientées client (DataGenerator génère des lignes, ValueProvider fournit des valeurs, Exporter exporte).

   *DIP — Dependency Inversion Principle
   Les modules haut-niveau (DatasetGenerator) dépendent d'abstractions (DataGenerator), pas d'implémentations concrètes.

 3. Fonctionnalités
    - Gestion des projets: création, ajout d'entités et sous-entités.
    -Définition des attributs: type, contraintes (min/max).
    -Génération de données: aléatoire respectant contraintes.
    -Export: CSV, JSON, XML, SQL. Facilement extensible.

 4. Extension future
    - Ajouter de nouveaux types de données (Date, Enum, etc.).
    -Ajouter de nouveaux formats d’export (Excel, YAML).
    -Connecter à des API pour valeurs réalistes (noms, villes).

 5. Utilisation
   -Créer un projet.
   -Ajouter des entités et attributs.
   -Générer le dataset avec `DatasetGenerator`.
   -Exporter avec l’un des exporteurs (`CSVExporter`, `JSONExporter`, SQLExporter,XMLExporter).




